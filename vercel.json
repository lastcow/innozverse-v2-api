{
  "version": 2,
  "buildCommand": "cd packages/database && npx prisma generate",
  "builds": [
    {
      "src": "apps/api/vercel-serverless.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "apps/api/vercel-serverless.js"
    }
  ]
}
