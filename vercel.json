{
  "version": 2,
  "buildCommand": "npx turbo run build --filter=@repo/api",
  "builds": [
    {
      "src": "apps/api/api/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "apps/api/api/index.js"
    }
  ]
}
